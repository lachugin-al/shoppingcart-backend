build:
  maxIssues: 10

processors:
  active: true

config:
  validation: true

complexity:
  LongMethod:
    active: true
    threshold: 110

style:
  MagicNumber:
    active: false
  MaxLineLength:
    active: true
    maxLineLength: 120

naming:
  ClassNaming:
    active: true
    classPattern: '[A-Z][a-zA-Z0-9]*'